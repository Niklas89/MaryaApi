type booking = {
    id: number,
    appointementDate: string,
    nbHours: number,
    description: string,
    accepted: boolean,
    totalPrice: number,
    cancelDate: string,
    isCancelled: number,
    serviceDone: number,
    createdAt: string,
    updatedAt: string
};

export default booking;