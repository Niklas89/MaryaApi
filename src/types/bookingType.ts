type Booking = {
    id: number,
    appointmentDate: string,
    nbHours: number,
    description: string,
    accepted: boolean,
    totalPrice: number,
    cancelDate: string,
    isCancelled: number,
    serviceDone: number,
    createdAt: string,
    updatedAt: string
};

export default Booking;