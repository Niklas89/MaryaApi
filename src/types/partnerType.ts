type Partner = {
  idPartner: number,
    phone: string,
    birthdate: string,
    address: string,
    postalCode: string,
    city: string,
    img?: string,
    SIRET: string,
    IBAN: string,
  };

  export default Partner;